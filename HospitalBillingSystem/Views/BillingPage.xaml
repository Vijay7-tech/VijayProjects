<Page x:Class="HospitalBillingSystem.Views.BillingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Billing Management">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Billing Management" FontSize="28" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,10">
            <Button Content="New OP Billing" Width="130" Height="35" Margin="0,0,10,0" Click="BtnNewOP_Click" Background="#2196F3" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="New IP Billing" Width="130" Height="35" Margin="0,0,10,0" Click="BtnNewIP_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Refresh" Width="100" Height="35" Margin="10,0,0,0" Click="BtnRefresh_Click" Background="#FF9800" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

        <DataGrid x:Name="dgBillings" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" CanUserAddRows="False" AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="100"/>
                <DataGridTextColumn Header="Type" Binding="{Binding BillingType}" Width="60"/>
                <DataGridTextColumn Header="Patient" Binding="{Binding Patient.FullName}" Width="150"/>
                <DataGridTextColumn Header="Doctor" Binding="{Binding Doctor.FullName}" Width="150"/>
                <DataGridTextColumn Header="Date" Binding="{Binding BillingDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=₹{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Payment Status" Binding="{Binding PaymentStatus}" Width="120"/>
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="View" Width="60" Margin="2" Click="BtnView_Click" Tag="{Binding Id}" Background="#9C27B0" Foreground="White" Cursor="Hand"/>
                                <Button Content="PDF" Width="60" Margin="2" Click="BtnPDF_Click" Tag="{Binding Id}" Background="#FF5722" Foreground="White" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
