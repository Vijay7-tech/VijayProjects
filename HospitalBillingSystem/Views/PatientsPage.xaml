<Page x:Class="HospitalBillingSystem.Views.PatientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Patients Management">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Patients Management" FontSize="28" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,10">
            <TextBox x:Name="txtSearch" Width="300" Height="35" FontSize="14" Padding="5" VerticalContentAlignment="Center"/>
            <Button Content="Search" Width="100" Height="35" Margin="10,0,0,0" Click="BtnSearch_Click" Background="#2E7D32" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Add New Patient" Width="150" Height="35" Margin="20,0,0,0" Click="BtnAddPatient_Click" Background="#2196F3" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Refresh" Width="100" Height="35" Margin="10,0,0,0" Click="BtnRefresh_Click" Background="#FF9800" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

        <DataGrid x:Name="dgPatients" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" CanUserAddRows="False" AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Patient ID" Binding="{Binding PatientId}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="150"/>
                <DataGridTextColumn Header="DOB" Binding="{Binding DateOfBirth, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="80"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Blood Group" Binding="{Binding BloodGroup}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Width="60" Margin="2" Click="BtnEdit_Click" Tag="{Binding Id}" Background="#4CAF50" Foreground="White" Cursor="Hand"/>
                                <Button Content="Delete" Width="60" Margin="2" Click="BtnDelete_Click" Tag="{Binding Id}" Background="#F44336" Foreground="White" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
