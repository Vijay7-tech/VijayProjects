<Page x:Class="HospitalBillingSystem.Views.PharmacyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Pharmacy Management">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Pharmacy Management" FontSize="28" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,10">
            <Button Content="Add Medicine" Width="120" Height="35" Margin="0,0,10,0" Click="BtnAddMedicine_Click" Background="#2196F3" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="New Sale" Width="100" Height="35" Margin="0,0,10,0" Click="BtnNewSale_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="View Sales" Width="100" Height="35" Margin="0,0,10,0" Click="BtnViewSales_Click" Background="#FF9800" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Refresh" Width="100" Height="35" Margin="10,0,0,0" Click="BtnRefresh_Click" Background="#9C27B0" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

        <DataGrid x:Name="dgMedicines" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" CanUserAddRows="False" AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding MedicineCode}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Generic Name" Binding="{Binding GenericName}" Width="150"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="120"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=₹{0:N2}}" Width="80"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="70"/>
                <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Width="60" Margin="2" Click="BtnEdit_Click" Tag="{Binding Id}" Background="#4CAF50" Foreground="White" Cursor="Hand"/>
                                <Button Content="Delete" Width="60" Margin="2" Click="BtnDelete_Click" Tag="{Binding Id}" Background="#F44336" Foreground="White" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
